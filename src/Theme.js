import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    dataSetTheme: {
      light: '#e3f2fd',
      main: '#f44336',
      dark: '#f57c00',
      contrastText: '#388e3c',
      mode: 'dark',
    },
  },
});